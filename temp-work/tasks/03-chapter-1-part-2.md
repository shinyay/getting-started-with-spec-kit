# Task 03: Chapter 1 Part 2 — Sections 1.4–1.6 (13 slides)

> **Scope:** `specify` CLI (5 slides) + Directory Structure (4 slides) + Command System (4 slides)
> **Plan reference:** `slides/SLIDE-PLAN-EN.md` lines 97–123
> **JP reference:** `slides/spec-kit-presentation.md` lines 542–870

---

## Section 1.4: `specify` CLI — Installation & Basic Commands (5 slides)

### Slide 1-23: Installing the `specify` CLI

**Plan ref:** Line 101 | **JP ref:** Lines 542–570

```markdown
---

## Section 1.4: `specify` CLI

# Installing the `specify` CLI

### Method 1: Dev Container (Recommended)

```bash
# Dev Container / GitHub Codespaces — pre-installed
# Just start the container and you're ready
```

### Method 2: Local Installation

```bash
# Prerequisites: Python 3.11+, uv, git, Node.js LTS
uv tool install specify-cli \
  --from git+https://github.com/github/spec-kit.git
```

> Verify installation: `specify version`
```

**Notes:**
- Two h3 sub-sections
- Code blocks for each method (short — 2 and 3 lines)
- Blockquote tip at bottom

---

### Slide 1-24: 3 CLI Commands

**Plan ref:** Line 102 | **JP ref:** Lines 570–600

```markdown
---

## Section 1.4: `specify` CLI

# 3 CLI Commands

| Command | Purpose |
|---|---|
| `specify init` | Initialize project — download templates + generate directories |
| `specify check` | Verify required tool installations (git, AI tools, editor) |
| `specify version` | Show CLI/template version + system info |

> All three are terminal commands — NOT Copilot Chat commands
```

**Notes:**
- Simple 3-row × 2-column table
- Blockquote to clarify: CLI commands ≠ Copilot Chat commands

---

### Slide 1-25: `specify init` — Options in Detail

**Plan ref:** Line 103 | **JP ref:** Lines 600–640

```markdown
---

## Section 1.4: `specify` CLI

# `specify init` — Options in Detail

```bash
# Create new project directory
specify init my-project --ai copilot

# Initialize existing directory
specify init . --ai copilot --force
```

### `--ai` options (20+)
`copilot` · `claude` · `gemini` · `cursor-agent` · `codex` · `windsurf` · `qwen` · `opencode` · `kilocode` · `auggie` · `roo` · `codebuddy` · `amp` · `shai` · `q` · `bob` · `qodercli` · `agy` · `generic`

### Additional flags
- `--ai-skills` — Prompt.MD templates (SKILL.md)
- `--script ps` — PowerShell scripts (Windows)
- `--no-git` — Skip Git initialization
- `SPECIFY_FEATURE` env var — feature detection override for non-Git repos
```

**Notes:**
- Code block with 2 init examples (4 lines)
- Agent list as inline code separated by `·`
- 4 additional flags as bullets (includes R11 SPECIFY_FEATURE enhancement)

---

### Slide 1-26: `--ai generic` — Bring Your Own Agent

**Plan ref:** Line 104 | **JP ref:** Lines 618–650

```markdown
---

## Section 1.4: `specify` CLI

# `--ai generic` — Bring Your Own Agent

```bash
specify init my-project --ai generic \
  --ai-commands-dir .myagent/commands/
```

| | Normal Agent | Generic |
|---|---|---|
| **`specify check`** | Validates agent installed | No check needed |
| **Directory** | Agent-specific path | Custom `--ai-commands-dir` |
| **Configuration** | Pre-defined | None required |

> **SDD is a process, not tied to any specific tool — technology independence proven**
```

**Notes:**
- R2 enhancement — dedicated slide for `--ai generic`
- Code block (2 lines)
- Comparison table (3 rows × 3 columns)
- Blockquote with key message (bold)

---

### Slide 1-27: `specify check` — Environment Diagnostics

**Plan ref:** Line 105 | **JP ref:** Lines 650–680

```markdown
---

## Section 1.4: `specify` CLI

# `specify check` — Environment Diagnostics

```
$ specify check

● Git                (available)
○ GitHub Copilot     (IDE-based — cannot check from CLI)
● Claude Code        (not found)
● Node.js            (v20.11.0)
● Python             (3.12.3)
```

> **See at a glance what's available.**
> Troubleshoot: `uv tool install specify-cli --force`
```

**Notes:**
- Simulated terminal output in code block (7 lines)
- `●` = checked, `○` = cannot check
- Blockquote for troubleshooting tip

---

## Section 1.5: Directory Structure Generated by `specify init` (4 slides)

### Slide 1-28: Project Structure After `specify init`

**Plan ref:** Line 111 | **JP ref:** Lines 680–720

```markdown
---

## Section 1.5: Directory Structure

# Project Structure After `specify init`

```
my-project/
├── .specify/         ← Scripts & templates
│   ├── scripts/
│   └── templates/
├── .github/          ← AI agents & prompts
│   ├── agents/
│   └── prompts/
├── .vscode/          ← VS Code settings
│   └── settings.json
└── constitution.md   ← Your project's DNA
```

> **Three directories form Spec Kit's scaffolding**
```

**Notes:**
- Directory tree in code block (10 lines — at the limit)
- Arrow annotations after each directory
- Blockquote summary

---

### Slide 1-29: `.specify/` Directory — Scripts & Templates

**Plan ref:** Line 112 | **JP ref:** Lines 720–760

```markdown
---

## Section 1.5: Directory Structure

# `.specify/` Directory — Scripts & Templates

### `scripts/bash/`
- `check-prerequisites.sh` — verify tool installations
- `common.sh` — shared utilities
- `create-new-feature.sh` — scaffold a new feature
- `setup-plan.sh` — initialize planning artifacts
- `update-agent-context.sh` — refresh agent context

### `templates/`
- `constitution-template.md` · `spec-template.md`
- `plan-template.md` · `tasks-template.md`
- `checklist-template.md` · `agent-file-template.md`

> **Templates are the blueprints for SDD artifacts**
```

**Notes:**
- Two h3 sub-sections (scripts and templates)
- 5 scripts with descriptions
- 6 templates listed (2 per line to save space)
- Blockquote for key message

---

### Slide 1-30: `.github/` Directory — AI Agents & Prompts

**Plan ref:** Line 113 | **JP ref:** Lines 760–800

```markdown
---

## Section 1.5: Directory Structure

# `.github/` Directory — AI Agents & Prompts

| Phase | Agent File | Prompt File |
|---|---|---|
| Constitution | `speckit.constitution.agent.md` | `speckit.constitution.prompt.md` |
| Specify | `speckit.specify.agent.md` | `speckit.specify.prompt.md` |
| Clarify | `speckit.clarify.agent.md` | `speckit.clarify.prompt.md` |
| Plan | `speckit.plan.agent.md` | `speckit.plan.prompt.md` |
| Tasks | `speckit.tasks.agent.md` | `speckit.tasks.prompt.md` |
| Implement | `speckit.implement.agent.md` | `speckit.implement.prompt.md` |

> **`agent.md` = agent mode · `prompt.md` = slash command**
```

**Notes:**
- 6-row × 3-column table showing agent/prompt correspondence (at the row limit)
- Does NOT include analyze, checklist, taskstoissues here (covered in optional commands slide)
- Blockquote for key distinction

---

### Slide 1-31: `.vscode/settings.json` — VS Code Integration

**Plan ref:** Line 114 | **JP ref:** Lines 800–830

```markdown
---

## Section 1.5: Directory Structure

# `.vscode/settings.json` — VS Code Integration

```json
{
  "chat.promptFilesRecommendations": [
    "speckit.constitution",
    "speckit.specify",
    "speckit.plan",
    "speckit.tasks",
    "speckit.implement"
  ]
}
```

> **Just open VS Code and `/speckit.*` commands are ready in Copilot Chat**

- Auto-recommends Spec Kit commands in Copilot Chat
- Auto-approves `.specify/scripts/` terminal execution
```

**Notes:**
- JSON code block (8 lines)
- Blockquote for key benefit
- 2 bullet descriptions
- The `chat.tools.terminal.autoApprove` setting is mentioned in bullets

---

## Section 1.6: `/speckit.*` Command System — Full Reference (4 slides)

### Slide 1-32: Core Commands (6) — Mapped to the 6 SDD Phases

**Plan ref:** Line 120 | **JP ref:** Lines 830–855

```markdown
---

## Section 1.6: Command System

# Core Commands (6) — Mapped to the 6 SDD Phases

| # | Command | Purpose |
|---|---|---|
| 1 | `/speckit.constitution` | Generate constitution (project principles) |
| 2 | `/speckit.specify` | Define spec + `[NEEDS CLARIFICATION]` markers |
| 3 | `/speckit.clarify` | Resolve ambiguities in the spec |
| 4 | `/speckit.plan` | Architecture + data model |
| 5 | `/speckit.tasks` | Task breakdown + ordering |
| 6 | `/speckit.implement` | Generate code from specs |

> **Each command runs in Copilot Chat** — not the terminal
```

**Notes:**
- 6-row × 3-column table (at the row limit)
- Numbered to match the 6 phases
- Blockquote clarification: Chat commands, not CLI commands

---

### Slide 1-33: Optional Commands (3) — Quality Enhancement

**Plan ref:** Line 121 | **JP ref:** Lines 855–878

```markdown
---

## Section 1.6: Command System

# Optional Commands (3) — Quality Enhancement

| Command | When to Use | Purpose |
|---|---|---|
| `/speckit.clarify` | Before Plan | Resolve ambiguity proactively |
| `/speckit.analyze` | After Tasks | Cross-artifact consistency check |
| `/speckit.checklist` | After Plan | Generate quality checklist |

> **Core commands are required; optional commands enhance quality.**

- Special: `/speckit.taskstoissues` converts `tasks.md` to GitHub Issues
```

**Notes:**
- 3-row × 3-column table
- Blockquote for core vs optional distinction
- Extra bullet for taskstoissues

---

### Slide 1-34: Recommended Execution Flow

**Plan ref:** Line 122 | **JP ref:** Lines 878–910

```markdown
---

## Section 1.6: Command System

# Recommended Execution Flow

```
/speckit.constitution
    ↓
/speckit.specify
    ↓
(opt) /speckit.clarify
    ↓
/speckit.plan  →  (opt) /speckit.checklist
    ↓
/speckit.tasks  →  (opt) /speckit.analyze
    ↓
/speckit.implement
```

> Special: `/speckit.taskstoissues` — convert tasks.md to GitHub Issues at any time
```

**Notes:**
- Flow diagram in code block (10 lines — at the limit)
- Arrow `→` for optional branches
- Blockquote for special command

---

### Slide 1-35: 3 Modes — prompt.md / agent.md / SKILL.md

**Plan ref:** Line 123 | **JP ref:** Lines 910–920

```markdown
---

## Section 1.6: Command System

# 3 Modes — prompt.md / agent.md / SKILL.md

| Mode | File Type | Usage |
|---|---|---|
| **Slash Command** | `*.prompt.md` | `/speckit.specify` in Copilot Chat |
| **Agent Mode** | `*.agent.md` | Long-running tasks in Copilot Agent |
| **Skills** | `SKILL.md` | Install as agent skills (`--ai-skills`) |

> **Same SDD phase, three execution modes.**
> This workshop uses slash commands (`prompt.md`).
```

**Notes:**
- 3-row × 3-column comparison table
- Blockquote clarifying which mode the workshop uses

---

## ✅ Chapter 1 Part 2 Quality Checks

- [ ] Section 1.4 has exactly 5 slides (1-23 through 1-27)
- [ ] Section 1.5 has exactly 4 slides (1-28 through 1-31)
- [ ] Section 1.6 has exactly 4 slides (1-32 through 1-35)
- [ ] `--ai generic` has a dedicated slide (1-26) with comparison table
- [ ] `SPECIFY_FEATURE` env var is mentioned in slide 1-25
- [ ] Agent/prompt correspondence table has 6 rows (core commands only)
- [ ] Execution flow diagram shows optional branches
- [ ] All code blocks ≤ 10 lines
- [ ] All tables ≤ 6 rows × 4 columns
- [ ] All h2 labels correct: `## Section 1.4`, `## Section 1.5`, `## Section 1.6`
- [ ] No Japanese text
